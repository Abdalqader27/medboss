import 'package:design/design.dart';

class EmptyBotFolderWidget extends StatelessWidget {
  const EmptyBotFolderWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
